spring:
  cloud:
    config:
      allowOverride: true
      overrideNone: true
      overrideSystemProperties: false
server:
  port: 8001
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:2000/eureka
