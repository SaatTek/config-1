eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:2000/eureka
server: 
  port:7200
spring:
  cloud:
    config:
      allowOverride: true
      overrideNone: true
      overrideSystemProperties: false
