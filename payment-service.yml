eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:2000/eureka
spring:
  cloud:
    config:
      allowOverride: true
      overrideNone: true
      overrideSystemProperties: false
